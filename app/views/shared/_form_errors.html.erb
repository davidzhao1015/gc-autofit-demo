<% if entry.errors.any? %>
  <div class="error-explanation alert alert-danger">
    <h2><%= pluralize(entry.errors.count, "error") %> prohibited this job from being started:</h2>

    <ul>
    <% entry.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
