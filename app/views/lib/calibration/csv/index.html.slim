= render(:partial => 'index', 
			:locals => {csv_files: @csv_files,
						base_file: @base_file,
						file: @file,  
						header: @header, 
						csv_rows: @csv_rows,
						type: @type,
						index_path: 'lib_db_path',
						domain: 'lib'} )